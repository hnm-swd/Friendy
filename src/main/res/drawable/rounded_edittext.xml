<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Màu nền -->
    <corners android:radius="12dp" /> <!-- Bo góc -->
    <stroke
        android:width="1dp"
        android:color="#CCCCCC" /> <!-- Đường viền -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" /> <!-- Padding bên trong -->
</shape>
